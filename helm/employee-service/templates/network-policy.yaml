apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: employee-default-deny
  namespace: {{ .Release.Namespace }}

spec:
  podSelector:
    matchLabels:
      app: employee-service

  policyTypes:
    - Ingress